'use client';

/**
 * หน้าสุ่มรายชื่อ (Public Page)
 * 
 * หน้าที่:
 * - แสดงการสุ่มรายชื่อแบบเรียลไทม์
 * - ใช้ WebSocket เพื่ออัปเดตผลแบบทันที
 * - แสดงผลแบบ Fullscreen สำหรับ Projector
 * - ไม่ต้อง Login
 * 
 * Features:
 * - แสดงรายชื่อที่กำลังสุ่ม
 * - Animation เมื่อมีการสุ่ม
 * - แสดงผลผู้ที่ถูกสุ่มออกมา
 * - รองรับ Dark Mode สำหรับ Projector
 */
export default function RaffleDrawPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-900 to-cyan-900 text-white p-8">
      <div className="max-w-6xl mx-auto">
        <h1 className="text-5xl font-bold text-center mb-8">🎲 สุ่มรายชื่อ</h1>
        <div className="bg-white/10 backdrop-blur-lg rounded-xl p-8 text-center">
          <p className="text-xl text-gray-300">
            หน้านี้จะแสดงการสุ่มรายชื่อแบบเรียลไทม์
          </p>
          <p className="text-sm text-gray-400 mt-4">
            (กำลังพัฒนา - จะเชื่อมต่อกับ WebSocket เพื่อแสดงผลแบบทันที)
          </p>
        </div>
      </div>
    </div>
  );
}

